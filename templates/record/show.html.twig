{% extends 'base.html.twig' %}

{% block title %}Record
{% endblock %}

{% block body %}
	<h1>Record</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ record.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ record.name }}</td>
			</tr>
			<tr>
				<th>Rate</th>
				<td>{{ record.rate }}</td>
			</tr>
			<tr>
				<th>Remark</th>
				<td>{{ record.remark }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-primary m-2" href="{{ path('app_record_index') }}">back to list</a>


	<a class="btn btn-primary m-2" href="{{ path('app_record_edit', {'id': record.id}) }}">Edit</a>


	{{ include('record/_delete_form.html.twig') }}
{% endblock %}
